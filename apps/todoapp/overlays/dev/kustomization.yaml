apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namePrefix: dev-

images:
  - name: aravindkotty/todoapp
    newTag: latest     # Jenkins updates this line with BUILD_NUMBER
